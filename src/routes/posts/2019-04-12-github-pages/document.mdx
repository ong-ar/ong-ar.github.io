import styles from "./document.module.css";
import create_repository from "./images/create-repository.png";
import create_index_html from "./images/create-index-html.png";
import select_github_pages_repository from "./images/select-github-pages-repository.png";
import check_github_pages from "./images/check-github-pages.png";
import confirm_github_pages_url from "./images/confirm-github-pages-url.png";

<div className={styles["Welcome"]}>

## github pages 란?

github 에서 제공해주는 무료 웹호스팅이다.  
웹호스팅 주소: **`{username}`**`.github.io/`**`{repository}`**  
html 이나 SPA 와 같은 정적 페이지에 적용이 가능하다

## 이용 방법

### 1. repository 생성

<img src={create_repository} />

---

### 2. index.html 파일 생성

<img src={create_index_html} />

---

### 3. master branch 를 github pages 로 선택

- 프로젝트 setting 에 진입하여 github pages branch 로 master 를 선택
- repository 생성 시 **`{username}`**`.github.io` 이외의 이름은 github pages branch 를 다른 것으로 설정 가능
- **`{username}`**`.github.io` repository 는 github pages branch 가 master 만 가능

<img src={select_github_pages_repository} />

---

### 4. github branch 변경 및 url 생성 확인

<img src={check_github_pages} />

---

### 5. 사이트 확인

<img src={confirm_github_pages_url} />

## 사용 제한

- github pages repository 권장 용량은 1GB
- github pages 사이트 용량은 1GB
- 트래픽은 한달에 100GB
- 사이트 빌드는 1시간에 10번으로 제한

</div>
